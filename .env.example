# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# OpenAI Pricing (per 1M tokens) — обновляйте при изменении тарифов
GPT4O_INPUT_PRICE=2.50
GPT4O_OUTPUT_PRICE=10.00
GPT4OMINI_INPUT_PRICE=0.15
GPT4OMINI_OUTPUT_PRICE=0.60

# LangSmith (опционально, для tracing)
LANGSMITH_API_KEY=ls-...
LANGSMITH_PROJECT=project0-pipeline
LANGSMITH_TRACING_V2=true

# LangGraph checkpointing (https://docs.langchain.com/oss/python/langgraph/persistence)
# CHECKPOINT_BACKEND: memory | sqlite (default) | postgres
CHECKPOINT_BACKEND=sqlite
CHECKPOINT_DB=checkpoints.db
# DATABASE_URL=postgresql://user:pass@localhost:5432/pipeline_db  # только для postgres

# Budget
DEFAULT_BUDGET_USD=5.0

# App
ENV=local
LOG_LEVEL=INFO
